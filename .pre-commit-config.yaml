repos:
  - repo: local
    hooks:
      - id: sops-encrypted-secrets
        name: Verify SOPS encryption
        language: system
        entry: make -f deploy/Makefile check-secrets
        pass_filenames: false
        stages: [pre-commit]
